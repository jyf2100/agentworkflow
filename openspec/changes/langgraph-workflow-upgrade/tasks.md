# Tasks — langgraph-workflow-upgrade

> 实现步骤。每条对应 spec 的 requirement 与 design 的决策（D#）/风险（R#）/迁移 Phase。代码层 big bang、发布层渐进（Phase 0→4）。回退：任一 Phase 失败 → `unset PA_GRAPH_ORCHESTRATOR` 秒回 legacy。

## 1. Phase 0：依赖共存验证（前置 spike，2 周）

- [x] 1.1 `pyproject.toml` 锁版本加 `langgraph>=1.2.10,<2`（避撤回的 1.2.3/1.1.7；CVE 阈值：sqlite<3.0.1=CVE-2026-28277、checkpoint<4.0.0=CVE-2026-27794——pa 不引 checkpoint-sqlite，sqlite 仅间接依赖时关注；**不需要** langgraph-checkpoint-sqlite，依赖减负；+ 确认 pydantic 间接依赖版本，pa 不主动依赖，OQ2 已决 TypedDict）；`pip install -e ".[dev]"` 装到 miniconda3（D8）
- [x] 1.2 `scripts/test_graph_cron_path.py`：在极简 PATH（`/usr/bin:/bin` + run_cron.sh 补的 miniconda3）下 `import langgraph; import claude_agent_sdk` 断言通过（R1 / spec「cron 极简 PATH 可 import」）
- [x] 1.3 共存 spike：1-node graph 调 `pa-radar`，在 cron 极简 PATH 跑通 + SDK 0.2.128 + `sdk_compat_patch` 仍命中 + 隔离 state smoke（R1/R2）
- [x] 1.4 radar 产物 byte-identical 断言（graph 1-node vs `run_daily.stage_radar`）+ `quality.sh` 全绿

## 2. Phase 1：graph 骨架 + I/O 契约 + 通用 node 抽象（2 周）

- [x] 2.1 `scripts/graph_pa_contracts.py`：`NodeInput`（6 类字段）+ `NodeOutput` envelope（7 字段）+ `ArtifactHandle`（{kind, store, rel_path, digest, must_exist}）用 **TypedDict + 中心化验证函数**（`validate_node_output`，守 pa 纯 stdlib 风格，不主动依赖 pydantic，D4 / spec「统一 node I/O 契约」+「ArtifactHandle 路径契约」，OQ2 已决 TypedDict）；解 OQ3（digest 长期强制、tmp 可选）
- [x] 2.2 `scripts/graph_pa_state.py`：`TypedDict` graph state（只持可序列化：str/ArtifactHandle as dict/轮次计数器 int，绝对路径 node 内解析不入 state，R8）+ 轮次计数器字段（`prd_round`/`verify_round`，跨节点回环判上限用，D3）
- [x] 2.3 `scripts/graph_pa_nodes.py`：4 类通用 node 工厂骨架（`PersonaNode`/`DevLoopNode`/`MechanicalNode`/`GatewayNode`，D3 / spec「四类通用 node 抽象」）；`verdict` 只在 `PersonaNode` 工厂暴露写入
- [x] 2.4 `scripts/check_boundary.py` + `scripts/test_check_boundary.py`：lint 扫「非 persona node 产 verdict」+「裸 `path: str`」（D3/R6 / spec「verdict 仅 PersonaNode 可写」）
- [x] 2.5 `node_radar` 迁移：`MechanicalNode` 壳 import 复用 `run_daily.run_persona`（零重写逻辑主体），radar 配置实例化（spec「新加 stage 不写新 node 代码」）
- [x] 2.6 `scripts/test_graph_radar.py`：graph radar 产物 == `run_daily.stage_radar` 产物 byte-identical（Phase 1 判据）

## 3. Phase 2：7 stage 全迁 + journal 单写持久化（4 周，核心期）

- [x] 3.1 fetch node（3 个 persona 配置实例：github-repo/wechat-url/deepresearch）
- [x] 3.2 inject + prd node（prd 配 `PersonaNode`，critic revise 回环先占位）
- [x] 3.3 critic 子图（`PersonaNode` + revise 回环 round2，复用 `_critic_one`）
- [x] 3.4 verify 子图（`PersonaNode` revise 闭环 + 跨节点条件边回环 + `VERIFY_MAX_ROUNDS` 用满 → interrupted_pr **enum 终态**（非 interrupt，D5 撤 / spec「升人工路径保持机械硬门」））；接 ArtifactHandle 传 `install_log`（见 4.1）
- [x] 3.5 dispatch 子图（`DevLoopNode` + SDK dev loop + worktree + session 续接；迁 single_flight/circuit_breaker/merge_loop/reconcile 协同，留 2 周，R4 / spec「特化容错作纯函数库」）— 8 子任务完成（3.5a DevLoopNode / 3.5b dev_post / 3.5c verify 子图升级 / 3.5d admission+worktree / 3.5e publication baseline+terminal_emit / 3.5f slot+publish_gates/merge 骨架 / 3.5g 10 节点子图组装 / 3.5h 端到端集成）；graph_pa_dispatch.build_dispatch_subgraph() + DispatchSubState，baseline 路（flag 全 off）shadow parity proxy 端到端绿，legacy 完整（run_daily 不 import graph = D7）；完整 cutover drill + auto_merge canary 留 task 3.10/5.5
- [x] 3.6 report node（`MechanicalNode` 机械聚合所有 node 的 `obs` cost/turns/duration_ms/model/token_usage → 标准化可查询 metrics 文件，决策 M 路径 A / spec「统一 node I/O 契约」）— `_aggregate_obs`（totals 求和 + by_model 分组 + nodes 明细，drop-None 容错）+ `_report_op`（写 STATE_DIR/metrics_<stamp>.json 固定名可查询 + ArtifactHandle store=vault digest 强制）；4 类 node 工厂的 `update={"obs_log":[obs]}` reducer 自动累加 → report 读 state["obs_log"] 聚合；test_graph_report 9 测试绿 + check_boundary lint 过
- [x] 3.7 journal 单写持久化（node 内 `journal.append_event`(fsync) 先 → return state，**不用 Checkpointer**，D2 单写真源 / R3 / spec「journal 单写真源」）— commit_node 单收口点接 `_journal_append`（4 类 node 工厂全部 commit_node 调用点自动覆盖）；`_journal_path` 经 state 注入（`_ni` str 转发，空则 no-op = flag gating，对齐 ShadowJournal 三契约）；event_type=`node_committed` 非 status（不在 `_EVENT_STATUS_MAP`，reducer 观测-only 不改 status，与 `_sj_terminal` status 终态事件正交——节点级观测 vs 状态机级迁移）；event_id=`nc-<node_id>-<_ik>-r<round>` 确定性（**r2 review CRITICAL 修正**：node_id 来自工厂 `name` 参数注入 NodeInput.node_id 字段 graph_pa_contracts.py:141，每节点唯一——原 obs.node fallback 致 dispatch 9 个 mechanical 节点共享 event_id、崩溃恢复无法定位执行点；4 工厂加 name + 25 实例化点）；**观测层容错 try 收窄**（r2 review HIGH：只裹 append_event I/O，import/JournalEvent 构造在 try 外让代码缺陷报错 + stderr 暴露写失败守「无静默失败」，对齐 ShadowJournal loop_runtime.py:80-85；round_n 用 dict.get 默认值消 falsy 陷阱；删 payload.terminal 恒 None 死字段）；DispatchSubState 加 `_journal_path: str` 字段；test_graph_journal 7 测试绿（含 event_id 唯一 + stderr 信号断言）+ quality 全绿（1786 passed）+ check_boundary lint 过。**r2 review（4 专家：silent-failure-hunter/type-design-analyzer/python-reviewer/code-reviewer）P2 follow-up**：① payload.base="" 与 task 3.8 耦合（node_committed 作 journal 首事件时 reduce 推导空 base；3.8 须显式传 initial 或 _journal_append 填 _base）② reducer dedup 污染（node_committed 进 applied_event_ids + duplicate 写 last_transition_error 稀释状态机违反语义；改 loop_state.apply_event 违反 D6，独立 spec 评估）③ `_iter`/`_prd` 注入依赖父图（task 5.2）④ `_journal_path` 提升到基础 GraphState（task 3.8/5.2）⑤ event_id 命名空间 registry + NodeKind Enum 化（长期）
- [x] 3.8 崩溃恢复接 `recovery_cli → recover_iteration(journal)` 判 external_known + 重建 initial state → `graph.invoke(state, thread_id)` 续跑（节点幂等，靠 reconcile + idempotency_key，D3 三不变式 / spec「崩溃恢复走 journal 重建 initial state」）— 新模块 `graph_pa_recovery.py`：① `recover_dispatch` 调 `reconcile.recover_iteration`（**D1 复用不重写 reduce**，守 D6）→ `_rebuild_dispatch_state`（**D2 分层**：shell 输入上游重算 + coord 派生 `_coord`/`_sj`/`_iter`/`_prd`/`_journal_path` + `state["recovery"]` 摘要 dict）→ `_decide_action`（**D4 5 态**：published→completed / 其他终态→terminal / BLOCK→manual_block / STOP→budget_exhausted / RESUME|FORK|NEW_SESSION→resume）；② `_apply_session_params`（**D3** 翻译 decision.mode → `_cur_resume_session`/`_cur_fork_session`，对齐 `graph_pa_verify._redo_session_retry` L131-142）；③ `resume_dispatch` 端到端（completed/terminal/manual_block/budget_exhausted **不 invoke** 返 `{action,plan,reason}`；resume → `_apply_session_params` + `build_dispatch_subgraph().invoke(state)` re-invoke from START，无 Checkpointer）。**D5 D6 技术债不阻断**：base 显式传 `recover_iteration(base=shell_inputs["_base"])`（绕开 task 3.7 r2 P2① payload.base=""）；用 `plan.iteration_status`（绕开 P2② node_committed duplicate 污染 last_transition_error）。**前置 gate**：baseline coord（session_store/resolver/retry_budget 全 None）→ ValueError（崩溃恢复是 journal-driven 能力，baseline 走 legacy dispatch_one）；journal 中部损坏 → JournalCorruptionError 传播（fail-closed 需运维）。test_graph_recovery 13→**21 测试**绿（A: 5 态 + baseline reject + 无效 status fail-closed + missing shell fail-closed + journal corruption 传播；B: rebuild 字段；C: completed/manual_block 不 invoke + resume invoke+session + 真 re-invoke 集成 + journal corruption 双层 + nonempty targets；D: session 映射 RESUME/FORK/NEW + TOCTOU×2）+ quality **1807** 全绿 + check_boundary lint 过（非 persona node 不产 verdict）。**范围（与 3.7 一致）**：核心能力 + dispatch 子图级别验证；生产接线（run_daily graph 入口 + 主图 graph_pa.py + recovery_cli 续跑子命令）留 task 5.2 / Phase 3。**r-review（4 专家：silent-failure-hunter/type-design-analyzer/python-reviewer/code-reviewer，0 CRITICAL）P0×5/P1×4 全修 + P2 待处理**：P0 ①`_apply_session_params` 浅拷贝不 mutate 入参（immutability，返新 dict）②`_REQUIRED_SHELL` 完整校验（含 `_prof`，缺键→ValueError）废 `.get` 空串退化 ③无效 `iteration_status`（非 enum，暗示 reducer/journal 损坏）→ MANUAL_BLOCK fail-closed 不静默续跑 ④RESUME 二次 `session_store.load` 返 None/无 session_id → RuntimeError TOCTOU fail-closed（不静默降级 NEW_SESSION 丢 dev 进度）⑤journal 损坏→JournalCorruptionError 传播测试锁（recover+resume 双层 fail-closed）；P1 ⑥`resume_dispatch` 返 `ResumeOutcome` frozen dataclass（对称 action/plan/reason/result 契约，消 dict key 不对称陷阱）⑦类型注解 `TYPE_CHECKING import reconcile` + `_RecoveryCoord` Protocol（允 SimpleNamespace mock 又给静态检查）⑧targets 非空真 reconcile 路径测试（D3 exactly-once 卫生，空 targets 假安全）⑨真 `build_dispatch_subgraph().invoke` 集成测试（`_BASE` 对齐 e2e + mock `reconcile_side_effects` safe_to_retry 模拟合法恢复→pr_open）。**P2 待处理（独立 spec change）**：BudgetState frozen，`consume` 返新实例但 `coord.retry_budget` 是 frozen Coordinator 字段，调用结果未回写→sdk_retries_used 不递增；跨 3 模块既有模式（`graph_pa_recovery`/`graph_pa_verify._redo_session_retry` L141-142/`run_daily` L2508），根治需 Coordinator 持 mutable budget holder + 改 3 处测试 mock，本期注释标记
- [x] 3.9 `scripts/test_graph_topology.py`（7 stage 拓扑 + inject 条件/插入位置 + lo/hi 窗口 + 无 Checkpointer）+ `scripts/test_graph_state_serialize.py`（state 序列化可移植 + 轮次计数器，R8）+ `scripts/test_subgraph_to_record_mapping.py`（dispatch rec 21 字段 1:1 对照 dispatch_one L2046-2056，shadow parity 前置闸）— Phase 3 批 1 完：主图 `graph_pa.py`（build_main_graph 编译期条件加 node + 线性边 + main 镜像 run_daily argparse/lock）+ `graph_pa_aggregate.py`（6 包装 stage_X：fetch/radar/prd/inject/critic/report + dispatch 聚合）；GraphState 加 `_journal_path`（task 3.7 r2 P2④）；quality 1826 绿。**关键决策①critic 包装 stage_critic**（不循环 build_critic_subgraph——verdict 缺失/revise 异常边界与 stage_critic 有差异，镜像复杂；切子图留 follow-up）；**关键决策②dispatch 经 stage_dispatch worker hook 参数化**（run_daily 加 `worker=None` 可选参数 3 处：stage_dispatch/_dispatch_serial_by_repo/_run_group_serial，默认 _run_one byte-identical，零 drift 复用 stage_dispatch 外壳；D7 run_daily 不 import graph_pa_*）；**关键决策③_args Namespace 经 state 透传**（违 R8 但只 node 内读不经 reducer 序列化 + 崩溃恢复不重建，注释标明）
- [x] 3.10 每 stage byte-identical `run_graph_shadow_parity_drill_per_stage`（批 3，R7 防 Counter 假绿）— 双源逐 stage 比 `candidates`/`prd_manifest`/`prd_gate`/`dispatch` JSON 内容（semantic-identical：反序列化后 `==`，非裸字节，容 key 顺序/空白差异）；4 JSON stage 覆盖 7 逻辑 stage（fetch 走磁盘中转无 JSON 产物、inject 覆盖 prd_manifest、critic=prd_gate）。`test_graph_shadow_parity_per_stage_detects_field_drift` 验证「终态 Counter 假绿（同 status 分布）但 candidates 字段漂移 → per_stage 抓住」。Phase 2 判据 `matched=True` + N 次一致待批 4 canary 真双源数据双跑后坐实（批 3 函数 + 单测就绪，非真双跑）

## 4. 边界债修复（重构契机，D3/D4，可并 Phase 2）

- [ ] 4.1 install 藏输入根治（层 1）：`independent_verify` 的 `install_log` 写为 `ArtifactHandle(store=tmp, must_exist=True, digest)`，verify node 的 `NodeInput.upstream_artifacts` 强制含，pa-verify 经 handle 自行 Read 判断（spec「install_log 成规范 artifact」+「根治藏输入」）
- [ ] 4.2 3 处编排器产 `drop` 改 `triaged`（层 2）：prd 缺 path / critic 漏吐 verdict / revise 异常 → graph 编排器产 `triaged`（升人工，不替判死）（spec「编排器残缺输入改 triaged」）
- [ ] 4.3 扩 pa-verify schema 加 `verifier_signal`（附带 #3，去 `:2484` 硬编码，解锁 FORK 重试分支，可选）
- [ ] 4.4 prompt 预写规则（层 3）评估移入 persona contract（低优，可选）

## 5. Phase 3：feature_flag + cutover（2 周）

- [x] 5.1 `feature_flags.py` 加 `pa_graph_shadow` + `pa_graph_orchestrator`（镜像 `single_flight_serial_shadow`/`single_flight_auto_merge`，D7 / spec「feature_flag 渐进 cutover」）— LoopFlags 加两字段（默认 False，docstring 补 graph shadow→orchestrator 段）+ FLAGS_ENV_MAP 加 `PA_GRAPH_SHADOW`/`PA_GRAPH_ORCHESTRATOR`（域切割非 PA_LOOP_，镜像 single_flight/verify_anchor）；resolve_flags 三态自动获取。test_feature_flags +6（default_false/env_names_mapped 域切割/env_truthy/via_profile/env_overrides_profile + test_all_env_names_mapped 锁 13 条映射）
- [x] 5.2 `run_cron.sh` 加 `PA_GRAPH_ORCHESTRATOR` 分流（=1 → `graph_pa.py`，else → `run_daily.py`）— L50 exec 改 if/else（`${PA_GRAPH_ORCHESTRATOR:-}` 兜底 `set -u`）；unset 秒回 legacy run_daily（D7 flag off 完整保留）；bash -n 语法 OK + 分流模拟双分支验证（=1→graph_pa / unset→run_daily）
- [x] 5.3 `cutover.py` graph orchestrator 四重 gate + 双源 shadow parity（批 3）— 5.3a `resolve_graph_orchestrator_source`（shadow→parity→allowlist→flag 四重 gate，镜像 `resolve_learning_injections_source` 比 dispatch 三重更贴合——orchestrator 亦 shadow-gated；复用 `DispatchCutoverResult`，纯函数零 IO）+ 5.3b `run_graph_shadow_parity_drill`（双源 run_daily vs graph_pa 终态，镜像 `run_shadow_parity_drill`）+ `GraphShadowParityEvidence`/`run_graph_shadow_parity_evidence`（读双 `dispatch_{stamp}.json`）+ `_graph_shadow_parity_outcome`。**接入 `run_full_cutover_suite` runner（`CutoverDrillBundle` 字段 / outcomes 元组 / `_ALLOWED_OPEN_ITEMS` 软 open item）留批 4 task 5.6**——与 canary 真双源数据一起接入，避免批 3 改 8 维度 suite + 白名单 + 无真数据致 red。test_cutover +15（gate 6 分支含短路序 + parity 3 + evidence 2 + outcome 1 + per_stage 3）
- [x] 5.4 preflight 强制 `pa_graph_orchestrator=on` 必须先 `pa_graph_shadow`（镜像 single_flight_auto_merge⇒serial_shadow 依赖，spec「driven flag 依赖 shadow parity」）— coordinator `_FLAG_DEPENDENCIES` 加 `("pa_graph_orchestrator","pa_graph_shadow",...)`；preflight `getattr(flags, flag)` 自动生效（run_daily L2083 调用点无需改）。注：preflight 只查 flag 组合（orchestrator⇒shadow），N 次 parity 次数验证在批 3 task 5.3 cutover drill。test_coordinator +2（rejects orchestrator w/o shadow→blocked / accepts w/ shadow→ok）
- [x] 5.5 canary cc-web-control 单项目开 flag 跑 ≥3 cron（隔离 state + 临时 log + `unset PA_HEARTBEAT`，守 pa-test-no-dirty-data）。— **批 4 完成（脚本封装 + 单次 parity 坐实；真跑 ≥3 cron 待用户触发）**：`canary_graph_cutover.sh` 镜像 `canary_automerge.sh` 封装 graph orchestrator cutover canary（**非** merge/revert 闭环——验 **shadow parity**：graph_pa vs run_daily 双跑 dispatch 终态对照）。子命令 prep（无害前置：graph_pa/双 flag/preflight 依赖/run_cron 分流/graph 套件/parity 函数/profile 全 grep 守）+ shadow-run（双跑 legacy run_daily + graph graph_pa，默认 `--dispatch-skip-dev` 零 outward + 零 LLM 成本：inject→critic skip→dispatch 过准入不跑 dev loop，产双 `dispatch_{stamp}.json`）+ parity（`run_graph_shadow_parity_evidence` 终态 Counter + `run_graph_shadow_parity_drill_per_stage` 逐 stage byte-identical）+ verify（判据汇总）。守 pa-test-no-dirty-data（双 state_dir 隔离 `.project-auto/canary-graph-{legacy,graph}` + 临时 log + unset PA_HEARTBEAT + --no-notify）。**坐实端到端（单次 parity）**：硬 gate (a) `parity.matched=True`（dispatch 终态 Counter 一致）+ (c) `load_ok=True`（双源真跑非假绿）全过。**canary 发现**：per_stage byte-identical 双 state_dir 局限——`prd_path` 路径前缀（`canary-graph-legacy` vs `canary-graph`）+ `verify_round` 初始值约定（legacy `None` vs graph `_build_dispatch_shell=1`）必然 mismatch，**非真实编排回归**（25 字段 20 相同，2 差异皆非语义）；verify 判据 per_stage 规范化比对已完（task 5.5 follow-up 收口：`cutover._normalize_state_dir_prefix` 剥 state_dir 路径前缀 + `_normalize_initial_verify_round` 归一 None→1，真实字段漂移 + verify 递增仍抓，+4 测试守护）；per_stage 判据干净，待 ≥3 canary 坐实 matched=True 可升硬 gate。真跑 ≥3 cron（不同 stamp 重复 shadow-run+parity+verify）待用户触发坐实非偶然 → 移硬 gate（cutover.py `_ALLOWED_OPEN_ITEMS` 原子删三元组 + 测试守护）
- [x] 5.6 `cutover.run_full_cutover_suite` 接入 `graph_shadow_parity` 第 9 维 + 8→9 维 evidence 作发布门（Phase 3 判据：`overall_passed=True` + dispatch 段 durable 不变式守住 + graph shadow parity N 次 matched）— **M4-sfh（批 3 审查产出·接入清单）**：批 3 已建 `run_graph_shadow_parity_drill`/`_evidence`/`_per_stage`/`LoadFailureReport`/`_graph_shadow_parity_outcome`（cutover.py L787-1031）但**未接 runner**（留批 4 与 canary 真双源数据一起接，避免批 3 改 8 维度 suite + 白名单 + 无真数据致 red）。接入须同步 5 点，**漏任一 runner fail-closed**：① `_SUB_EVIDENCE_ALLOWLIST_BY_KIND`（L2043）加 `graph_shadow_parity` 字段白名单（未知 drill L2095 fail-closed）② `_EXPECTED_DRILL_NAMES`（L1719）加其名（接前现恰 8 个，L1738 漏/多跑 → 阻断）③ `CutoverDrillBundle`（L1673）加 `graph_shadow_parity` 字段 ④ `run_full_cutover_suite`（L2195）outcomes 元组加 `_graph_shadow_parity_outcome` 条目 ⑤ `_ALLOWED_OPEN_ITEMS`（L1761）若作软 open item 则加 `graph_shadow_parity`（否则红色 outcome 被 L1855 拒致 overall 假绿判定失败）。须 canary 真双源 `dispatch_{stamp}.json` 双跑坐实 `matched=True`（非空计数的真 ShadowParityReport，加载失败走 LoadFailureReport 不伪造）。— **批 4 完成（code review Ready-to-merge Yes）**：5 点接入全绿 + 6 个 `CutoverDrillBundle(` 构造点全传新字段（grep 证，零 TypeError ripple）+ 生产 `real_cutover_suite`/`real_cutover_drills(graph_state_dir=None)` 诚实 LoadFailureReport（短路在 `run_graph_shadow_parity_evidence` 前，非自比假绿）+ 3 新测试验真行为（soft open 不阻断 overall / 默认 LoadFailureReport / 双 dir 真读取 source_load_error）；quality 1863 绿。软 open 待 task 5.5 canary ≥3 cron 真双跑坐实 matched=True 后**原子**移硬 gate（r-review M1 防窗口，cutover.py open_items 块注释已标 ops step）
- [x] 5.7 critic 切子图（替批 1 包装 `stage_critic`，循环 `build_critic_subgraph`）— **完成**：`_critic_main_op` 切循环 `build_critic_subgraph`（每 PRD invoke，替 `stage_critic` 命令式 for+revise 回环），critic 段全 graph 化（D6 完整）。三边界全镜像 stage_critic L915-965：① 缺 path→drop 聚合层预过滤（L934-937，子图前短路不调 persona）② 漏吐 verdict→drop 聚合层后处理（L940-943，子图 route 对 `_critic_verdict=None` 静默 done，最后 entry 缺 verdict 时降级）③ revise 异常→drop 子图 `_revise_fn` try/except（L959-962，设 `_revise_failed`+`_critic_verdict=drop` → critic round2 `_critic_fn` 短路 no-op + route done，invoke 正常返回保 round1 entry 不丢，不穿透子图）。`_revise_fn` 同步修订后 `_prd_path`（对齐 L953-954 rev_path，否则 round2 过闸旧 PRD 失去修订意义）。复用门 + skip_critic 全 pass 在聚合层（skip 优先保 canary/演练语义）。shadow parity：prd_gate 内容 semantic-identical（子图 entry = dict(payload) 同 _critic_one payload 来源 pa-prd-critic；round/revised 字段对齐）。test_graph_critic +7（子图 revise 异常保 round1 / revise 同步 path + 聚合层缺 path→drop / 漏 verdict→drop / revise 异常→drop / skip_critic 全 pass / 复用门）= 15 点绿；quality 1872 全绿
- [x] 5.8 主图端到端集成测试（dispatch 聚合 `worker=_dispatch_one_graph` + `build_main_graph.invoke` 全 7 stage smoke）— **r-review 发现的测试缺口**：批 1 有子图级 e2e（test_graph_dispatch_e2e）+ 拓扑/映射单测，但缺「stage_dispatch(worker=_dispatch_one_graph) → 子图 invoke → rec」聚合级 + 主图级线性推进 smoke。批 3 shadow parity drill（3.10）是双源对照，本 task 补单源 graph 路径端到端 smoke（mock persona/dev subprocess，断言线性推进 + obs_log 累积 + metrics 落盘）。— **批 4 完成**：`test_graph_main_e2e.py` 两用例互补：**A** `test_main_graph_full_smoke`（`build_main_graph({lo:0,hi:6}).invoke` 全 6 包装 node 线性推进 fetch→radar→prd→critic→dispatch→report + obs_log 6 条累积冒泡 + `metrics_<stamp>.json` 落盘 node_count=5）+ **B** `test_dispatch_one_graph_worker_to_subgraph`（直调 worker `_dispatch_one_graph` → `_invoke_dispatch_subgraph`（shell 13 字段 `_REQUIRED_SHELL` + coord 派生注入）→ 子图 invoke mock → `_subgraph_result_to_record` rec 字段映射 + serial_shadow off 走 baseline nullcontext）。mock 守「只 mock 跨进程/跨网络/LLM 边界」（6 个 stage_X + 子图 invoke + 准入底层），包装 node op 真跑。**暴露并修 2 latent bug**（批 1 拓扑测试只验 `add_node` 边未真 `invoke`，主图 invoke 从未跑通）：① `GraphState` 缺 `_args`/`_sources`/`_profiles` 声明（langgraph 只透传声明字段 → 未声明 invoke 时被丢 → fetch node `KeyError`）② `graph_pa.py` `run_id=""` 空占位触发 `validate_node_input` `ContractError`（run_daily run_id=stamp 非空）。graph 套件 34 点全绿无回归 + quality 全绿

## 6. Phase 4：下旧（2 周）

- [ ] 6.1 flag 默认 True 留 1 release cycle（≥4 周 cron）观察（R10）
- [ ] 6.2 删 `run_daily.py` 的 `main()` / 命令式 `STAGES` 调度（**留** `stage_*` 纯函数 + `run_persona` 被 graph node import 复用，spec「claude runtime 不变」）
- [ ] 6.3 `run_cron.sh` 改 `exec graph_pa.py`（legacy 路径删）
- [ ] 6.4 历史 journal.jsonl 保留只读（审计）

## 7. 文档 + 收尾

- [ ] 7.1 `CLAUDE.md` 高层架构小节补 LangGraph 编排层（graph_pa*.py + feature_flag 分流 + journal 单写持久化 + 秒回退）
- [ ] 7.2 `RUNBOOK.md` 加 graph 恢复（`recovery_cli` over journal）+ shadow parity drill + 秒回退步骤
- [ ] 7.3 新增测试套全绿（test_graph_cron_path / test_graph_radar / test_graph_topology / test_graph_state_serialize / test_check_boundary / test_artifact_handle / test_shadow_parity）
- [ ] 7.4 `bash scripts/quality.sh` 最终全绿（compileall + pytest + ruff）+ OQ1-OQ4 决策落档（OQ1/OQ2 已决、OQ3 待 Phase 1、OQ4 撤）

## 8. follow-up（重构落地后，不在本期）

- [ ] 8.1 LangFuse 自托管标准化监控 sink（决策 M 路径 C）：软接入（fire-and-forget，服务挂了只 log 不 crash 流水线），obs 数据流不变只改 sink（report-file → LangFuse span）；前置 = 路径 A 的 obs schema 已稳定（见 3.6）
