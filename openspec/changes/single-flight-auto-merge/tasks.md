## 1. 准备：feature flag + 状态 schema + ADR

- [x] 1.1 在 `feature_flags.py` 注册 single-flight **双 flag**（`single_flight_serial_shadow` + `single_flight_auto_merge`，默认关）——遵循 pa shadow→driven pattern（单 bool flag 表达不了「串行+不merge」vs「串行+真merge」）；TDD RED→GREEN（23 测全过）+ ruff 干净
- [x] 1.2 扩展 `dispatch_*.json` schema：新增 `merge_commit` / `reverted` / `triage_reason` / `post_merge_verdict` 字段（向后兼容，旧字段保留）；TDD RED(KeyError)→GREEN(24 测过)+ruff 干净；rec 最早构造点即含新字段（preflight 阻断短路可断言）
- [x] 1.3 新 ADR `0008-control-plane-auto-merges-target-main.md`：8 护栏覆盖执行位置(D6/守 ADR-0001)、分支保护/CD 准入、auto-revert 充分性(D3)、exactly-once、flag gated——「pa 凭什么自动改 main」复核锚点

## 2. 串行单飞消费器（D1 / D5 / D9）

- [x] 2.1 改 dispatch 投递为同 `owner_repo` 串行消费（一次一个 PRD 走完 dev→verify→merge 闭环才下一个），跨 `owner_repo` 并行；复用并扩展现有 `DISPATCH_LOCKS`（已是整段闭环串行，本项是**显式化 + 跨进程化**，非全新实现）；新增 `_dispatch_serial_by_repo`（按 owner_repo 分组、同组顺序 _run_one、跨组 ThreadPoolExecutor 并行）+ stage_dispatch flag gated 路由；TDD RED→GREEN(27 测)+ruff
- [x] 2.2 改准入门为 per-repo single-flight slot 检查：slot 状态 = journal 在途闭环状态 + 跨进程 flock（**非** GitHub OPEN PR `count_inflight_prs`、非进程内 `threading.Lock`）；slot `UNKNOWN` → `blocked_external_state`，不当代空闲
- [x] 2.3 slot crash 恢复：journal 重放 + lease TTL 重建为 known（free 或 in-flight-with-lease），**不**默认 free
- [x] 2.4 `max_prs_in_flight` 退化为同项目恒 1（保留作 bug 安全阀）；`count_inflight_prs` 语义独立为「OPEN PR 上限」门（≠ slot），去留见 design Open Questions；dispatch_one 准入门 4 serial_shadow on→`_max_inflight=1`、off→baseline(prof 默认 2)；TDD RED(planned≠skip)→GREEN(36 测)+ruff

## 3. 自动 merge 阶段（D2 / D6 / D7）

- [x] 3.1 dev+verify 双绿 → fetch main HEAD + rebase 分支到当前 main，产出三态 `CLEAN` / `CONFLICT` / `UNKNOWN`（CLEAN 须**正向证据**：fetch 成功 + exit0 + 干净工作树 + 无冲突标记；缺证 = UNKNOWN） — 判定层 + rebase 执行均 ✓（`merge_phase.classify_rebase` + dev-agent `run_merge_phase` fetch→checkout branch→rebase（D10 timeout 120s）→收证→classify，quality 1328 passed）；dev+verify 双绿**触发接线**随 task #5 dispatch
- [x] 3.2 `CLEAN` → 在 worktree 内 `--no-ff` merge + push（ff-only，禁 `--force*`），记录 `merge_commit`（替换现「兜底开 PR 待 review」） — ✓ dev-agent `run_merge_phase`；**用机械层（``git()`` subprocess）非 SDK loop**：合并是确定性机械活，走机械层更稳/可测/不引入 agent 非确定性，符合 ADR-0001 机械 vs 语义切分（D6「在 dev-agent 内执行」已守——在 dev-agent 进程内跑 git，控制面只发 ``--phase merge`` cmd）；push reject→reset 本地 main + UNKNOWN（main unchanged）；dispatch 接线（替换兜底开 PR）随 task #5
- [ ] 3.3 `CONFLICT` / `UNKNOWN` → 转 triage 池，**不强合**（对齐 fail-safe UNKNOWN 不变式） — dev-agent 层已保：merge_commit=None + main 未碰 + ``triage_reason`` 给出 rebase_conflict/rebase_unknown；**triage 池落池机制**（枚举/不阻塞/成段）在 task 5.1
- [x] 3.4 给 merge commit 打稳定 marker（footer `Pipeline-Merge: <prd_id>`）供回滚后机械找出（`git log --grep`） — ✓ `run_merge_phase` 双 ``-m``（标题 + footer 段 ``Pipeline-Merge: <prd_id>``），可 ``git log --grep "Pipeline-Merge: <prd_id>"`` 找出

## 4. post-merge 验证 + auto-revert 兜底（D3 / D8 / D11）

- [x] 4.1 自动 merge 后经 dev-agent 对**集成后 main** 跑全量测试（基线 = main，与 verify 覆盖面/基线均不同；显式注释防被当冗余偷工）— **✓ apply 2026-07-30**：`dev-agent.py --phase post-merge-test`（机械 subprocess；`_run_test_cmd` 注入 conda env PATH via `sys.executable`）+ `merge_phase.classify_post_merge`/`parse_post_merge_result`；dispatch 接线 `run_daily._mr.merged` 块（`_post_merge_test_cmd`：Node→`scripts.test` / Python→`dev_test_cmd`）
- [x] 4.2 测试三态：`PASS`→保留+放行；`FAIL`→revert；`UNKNOWN`→keep+quarantine+halt+CRITICAL 告警（**不**自动 revert）— **✓ apply 2026-07-30**：`PostMergeVerdict.{PASS,FAIL,UNKNOWN}`（CLEAN 式全正向证据；ran/test_rc/timed_out）；PASS→`merged` / FAIL→revert(4.3) / UNKNOWN→`halted`+`_halt_slot_safe`(slot_halted 终态，下轮 blocked)。**CRITICAL 当前=非持久 log + rec 字段；durable 告警化留 4.5**
- [x] 4.3 revert 三态：`REVERTED`→放行+进 triage(reason=`post_merge_red_reverted`)；`CONFLICT`/`UNKNOWN`→halt 整仓队列 + CRITICAL 告警（**不** continue，**不**强改 main）— **✓ apply 2026-07-30**：`dev-agent.py --phase revert`（`git revert -m 1`+ff-only push+记 revert_commit；CONFLICT→`--abort` / push-reject·UNKNOWN→`reset --hard origin` 对齐远端）+ `classify_revert`/`parse_revert_result`；REVERTED→`triaged`(post_merge_red_reverted) / CONFLICT·UNKNOWN→`halted`+halt 整仓。离线 drill `test_dev_agent_merge.py` 6 测覆盖全链路（**核心安全网**：merge 合红→post-merge FAIL→revert REVERTED→main 回绿；+ CONFLICT/UNKNOWN）
- [x] 4.4 revert 循环熔断：同幂等键 PRD 被 post-merge revert 后 cooldown 窗口内禁再 auto-merge，强制进 triage — **✓ apply 2026-07-30**：`circuit_breaker.py`（`record_revert`/`is_in_cooldown`，per-owner_repo cooldown journal `state_dir/cooldown/<safe>.journal.jsonl`，复用 `append_event` 原子追加 + `JournalEvent` event_type=`revert_recorded`；默认窗口 7d，`window_seconds` 可覆盖）。键=`_prd`（`_coord.prd_id`，content-addressed，跨 cron 稳定；内容改→新键→放行重试）；dispatch 接线：REVERTED→`record_revert`、merge 前→`is_in_cooldown` 命中→`triage(cooldown_revert_loop)`+break（不 merge）。**fail-open**（额外保护层非 fail-safe 核心：journal 损坏/读失败→False 放行，对齐「block 需正向匹配证据」；durable 告警化留 4.5）。TDD 11 测（spec 两 scenario + 窗口边界 + 多记录 + fail-open + 跨读持久化）；quality 1383 passed
- [x] 4.5 告警 durable 化（journal 事件 + retry）；告警未确认 + revert 未确认 → 队列 halt — **✓ apply 2026-07-30（核心）**：`critical_alert.py`（`raise_alert`/`acknowledge_alert`/`pending_alerts`，独立 alerts journal `state_dir/alerts/<safe>.journal.jsonl`，复用 `append_event` 原子追加；**不受 `journal_shadow` flag gating**——CRITICAL 安全事件总 durable，crash 不丢，区别于 ShadowJournal）。未确认语义（ack 扣除 pending）。dispatch 接线：halt 2 出口（revert 非 REVERTED / post-merge UNKNOWN）→ `_raise_critical_alert_safe`（fail-open wrapper）。halt 队列安全已由 slot_halted 终态保证（4.2/4.3），告警 durable 是可观测+通知维度。**retry**：durable 落盘即 crash 不丢，发送 retry 由 report 段 `_smtp_notify` 聚合承载（crash 后下轮重发=天然 retry）；spec「复用 retry_policy.py」经评估语义错位（retry_policy 是 dev/verify iteration retry），故 SMTP 承载（design F5 注）。**follow-up（非阻断）**：report 段读 pending_alerts 成「CRITICAL 告警」段 + ack↔resume_slot 联动（人工 unblock 时 ack）。TDD 11 测（critical_alert 9 + wiring 2）；quality 1394 passed
- [x] 4.6 「main may be transiently red」契约 + 最大红窗上界（联 D10）+ 可查询的「main 是否已过 post-merge 验证」状态 — **✓ apply 2026-07-30**：`main_status.py`——`MAX_MAIN_RED_WINDOW_SECONDS=1800`（红窗上界=dev-agent `POST_MERGE_TEST_TIMEOUT`，D10；契约：main 在 [push, post-merge verdict] 窗口可能红，CD/分支保护须容忍或绑 verdict 而非 raw push）+ `record_main_verified`/`main_post_merge_status`（per-owner_repo `main_status/<safe>.journal.jsonl`，**不受 flag gating**；post-merge 判决后记 verdict/merge_commit，下游据此查「main 是否已过验证」）。dispatch 接线：post-merge 三态判决后 `record_main_verified`（PASS=已验证 / FAIL=曾红已revert / UNKNOWN=halt）。顺手修正 4.4 `record_revert` + 4.6 `record_main_verified` 真 fail-open（docstring 已承诺，加 try/except 不阻断 revert/判决流程）。TDD 9 测；quality 1403 passed

## 5. triage 池 + report 语义（D4）

- [x] 5.1 各 triage 出口进池不阻塞（除非 halt）；ejection reason 取自固定枚举（`timeout`/`verify_exhausted`/`rebase_conflict`/`rebase_unknown`/`push_failed`/`post_merge_red_reverted`/`post_merge_unknown`） — **✓ apply 2026-07-30**：`run_daily.TRIAGE_REASONS` frozenset 固定枚举（spec D4 七值 + 实现新增 `cooldown_revert_loop`/`post_merge_revert_conflict`/`post_merge_revert_unknown`；D3 halt 强于 D4 故 `post_merge_unknown` 实走 halted 而非 triage）。底层出口已就位（4.4 cooldown_revert_loop→triaged / 4.3 post_merge_red_reverted→triaged、revert 非 REVERTED→halted / 4.2 post_merge_unknown→halted / 3.x rebase_conflict·rebase_unknown·push_failed→triaged）。`timeout`/`verify_exhausted`（pre-merge 出口）预留待 dev/verify 阶段接 triage。防漂移：report 段聚合校验 triaged/halted rec 的 `triage_reason`∈枚举，漂移→log warning（fail-open 不阻断报告）
- [x] 5.2 report 简讯语义从「开 PR #X 待 review」→「已合 main（commit abc）」/「需 triage（原因）」；triage 单独成段，与已合/在途/halted 区分 — **✓ apply 2026-07-30**：`stage_report` 概览行加「已合 main / triage / halt」三计数；`_smtp_notify` subject + `_append_daily_pointer` 日报指针含三态（格式「词+数字」：已合N 待reviewN failingN 需triageN haltedN 阻断N）；三态各成独立段（✅已合main含 merge_commit sha / 🔧需triage含 ejection reason / 🛑halted含 reason），与 review/failing/drop/异常/阻断/完整性 区分
- [x] 5.3 `stage_report` 状态桶扩展（merged / reverted / triaged / halted），不只「在途/完成」 — **✓ apply 2026-07-30**：新增 merged/triaged/halted 三独立桶（`d.get("status")` 分类），不进 baseline 的 review/failing/abnormal/blocked/integrity 桶（防混计，测试锁「产出 PR:0 / failing:0 / 异常:0」）。注：「reverted」非独立终态——revert 是动作：REVERTED→`triaged`(post_merge_red_reverted)，CONFLICT/UNKNOWN→`halted`，故 reverted 归入 triaged/halted 桶（reason 区分），不单列。`active` 加 triaged/halted（运维须 triage：halted=CRITICAL / triaged=可人工 review 重试）；TDD 3 测（枚举完整性 + 三态桶渲染 + 漂移 fail-open）；quality 1406 passed

## 6. durable + spec 对齐（D12，拆细）

> **方案 C（halt+follow-up，用户 2026-07-30 选定）主防线 + follow-up 全部落地**：`merge_loop.py` dispatch 级
> crash 安全门（task 6.1d 写顺序 + has_open_intent 决断）防「merge push 后 crash → cron 重分发 → rebase CLEAN →
> 重复合 main」致命场景。6.1a 经评估**架构不适用**（merge/revert 走独立 merge_loop journal，不进 reducer）；
> 6.1b（reconcile merge/revert 种 + ancestry resolver）+ 6.1c（CRASH_BOUNDARIES + runtime_evidence 真实 SIGKILL
> drill 扩 7 边界）**已补全**（canary 前质量最硬的一块）。主防线 + 双层 reconcile/ancestry + crash drill 闭环完整。

- [~] 6.1a 状态机扩展：新 `MERGED` 非终态（现有 `published` 是终态 `_TRANSITIONS[PUBLISHED]=frozenset()` 会阻塞 revert；`MERGED` 须允许 → post-merge-verified / reverted 转移）— **架构不适用（apply 2026-07-30）**：reducer 的 `_EVENT_STATUS_MAP` 仅处理 dev/verify 迭代事件（planned/running/test/publish_ready/published），merge/revert 走**独立** merge_loop journal（`state_dir/merge_loop/<safe>.journal.jsonl`），从不进 reducer。故方案 C 的 `merge_loop.has_open_intent`（dispatch 级 crash 安全门）已直接覆盖 merge/revert 可见性，reducer MERGED 非终态在架构上多余——加它反而引入两套真相源（reducer 状态 vs merge_loop journal）。主防线落地后此节无需实现。
- [x] 6.1b reconcile：扩 `ALLOWED_KINDS` 加 `merge`/`revert`；resolver 三态——`merge` 查 `merge_commit` 是否 main 祖先；**`revert` 查 `revert_commit`（git revert 新产 commit，revert 时须记录 sha）是否 main 祖先**（⚠️ v2.1 修正：git revert 不删原 merge commit，故不能用 merge_commit presence/absence 判 revert 是否已发生）；FOUND→跳过重 apply，UNKNOWN→阻断 — **✓ apply 2026-07-30**：`ids.py`（`IDEMPOTENCY_MERGE`/`IDEMPOTENCY_REVERT` + `_IDEMPOTENCY_KINDS`）+ `reconcile.py`（`ALLOWED_KINDS` 加 merge/revert；`LocalGitResolver` 加 `main_ref` 参数 + `git merge-base --is-ancestor <target> <main_ref>` 三态：exit 0→True confirmed / exit 1→False absent / 其他→None unknown block）。TDD 7 测（白名单 + 幂等键 + merge ancestry + revert ancestry + main_ref 缺失→unknown + reconcile confirmed/unknown）；test_spec_durable_merge_revert.py 3 个 xfail strict 移除→真实 ancestry 端到端测（经 reconcile_side_effects + LocalGitResolver 真实 git repo，非 FakeResolver）；test_crash_injection/test_reconcile 原 `"merge"` illegal-kind 用例改 `"bogus_kind"`（merge 已合法化）。quality 1430 passed（xfail 8→5）。
- [x] 6.1c crash 边界：扩 `CRASH_BOUNDARIES` 加 `merge_push`/`revert_push`（子阶段独立 checkpoint，不只「闭环」整体；防 god-stage 整段重跑/放弃） — **✓ apply 2026-07-30**：`cutover.py`（`CRASH_BOUNDARIES` + `_BOUNDARY_TARGETS`[merge_push→merge_commit_sha / revert_push→revert_commit_sha] + `_CRASH_BOUNDARY_ORDER` 5→7 边界；3 处 docstring 同步）+ `runtime_evidence.py`（`real_crash_restart_drill` 真实 SIGKILL drill 扩 7 边界：真实 main `--no-ff` merge feat-branch→merge_commit、`git revert -m 1`→revert_commit、push main 到 origin 使 `refs/remotes/origin/main` 可供 ancestry check；targets 加 merge/revert 真实 sha）。实跑验证：7 边界全 exactly_once/safe_to_retry，merge/revert 经真实 `git merge-base --is-ancestor` 判 confirmed，merge_push/revert_push 真实子进程被 SIGKILL 后 restart 对账成功。TDD 6 测（merge_push/revert_push 各 confirmed/absent/unknown）。quality 1436 passed。
- [x] 6.1d 写顺序规约：journal intent(`merging`/`reverting`) → push → journal confirm(`merged`/`reverted`)；恢复器据两端事件 + resolver 决断 — **✓ apply 2026-07-30（方案 C 主防线，dispatch 级）**：`merge_loop.py`——per-owner_repo journal（`state_dir/merge_loop/<safe>.journal.jsonl`，复用 `append_event` 原子追加 O_APPEND+fsync）记 intent（`merge_started`/`revert_started`，phase 前）→ confirm（`merge_completed`/`merge_abandoned`/`revert_completed`，phase 后）；`has_open_intent`（最后事件是 started→True；**fail-safe：journal 损坏/读失败→True halt**，刻意区别于 circuit_breaker fail-open——破坏性副作用门不可放行）。dispatch merge 块顶部安全门：open intent→halt 整仓+CRITICAL（`merge_loop_open_intent`，绝不盲目重 merge）。TDD 13 测（record/last_event + has_open_intent 各状态 + prd/owner_repo 隔离 + 损坏 fail-safe）；quality 1419 passed。**resolver 增强（6.1b）follow-up**：当前决断纯据 journal 两端事件
- [x] 6.2 为 `single-flight-auto-merge`（全 scenario）/ `fail-safe-dispatch`(delta) / `verified-dev-execution`(delta) 的每个 scenario 写对应单测/集成测 — **✓ apply 2026-07-30（6.1b 后补全）**：新增 `test_spec_durable_merge_revert.py` 锁 fail-safe-dispatch delta「Exactly-once reconciliation of merge/revert」4 scenarios——scenario「Crash between intent and confirm」PASS（方案 C has_open_intent→halt 覆盖安全不变式：不 double-apply / 不丢副作用）；scenarios「Merge already applied」/「Revert via revert commit」/「Merge state unknown blocks」原 xfail strict（6.1b follow-up 锁）已随 6.1b 落地**移除**，改为真实 ancestry 端到端测（经 reconcile_side_effects + LocalGitResolver 真实 git repo；revert 场景另断言原 merge_commit 仍是 main 祖先——证 spec v2.1「不可由 merge_commit 推断 revert 状态」）。**其余 scenarios 已被 task 2-5 既有测试覆盖**：single-flight-auto-merge 13（serial single-flight 3→test_single_flight*.py task2.x / automated merge 3 + three-state rebase 3→test_merge_phase.py+test_dev_agent_merge.py task3.x / post-merge+auto-revert 4→test_dev_agent_merge.py task4.x / triage pool 2→test_report.py task5.x）；fail-safe-dispatch admission 4（既有 fail-safe-dispatch 测 task2.x）；verified-dev-execution delta 5（既有 verified-dev-execution 测）。xfail 总数 8→5（-3，6.1b 落地后 XPASS strict 移除）

## 7. 验证 + 灰度上线

- [x] 7.1a shadow 模式：串行消费器全跑但只 log、不真 merge/push，对齐 shadow parity（`cutover.py` 产出证据） — **✓ apply 2026-07-30**：兑现 `feature_flags` docstring「merge/revert 只 log 不改 main」承诺（先前 docstring 与实现矛盾=缺陷）。shadow（`serial_shadow=on, auto_merge=off`）时 dispatch verify-绿 else 分支跑 **classify-only** rebase 记 shadow 决策，**不 merge/push**（main 不碰）：① `merge_phase.build_classify_cmd`（控制面发 `--phase merge --classify-only`）② `dev-agent run_merge_phase` CLEAN 短路（`--classify-only` → `rebase --abort` 还原分支 + emit CLEAN+`merge_commit=None`，CONFLICT/UNKNOWN 行为不变本就不 merge）③ `run_daily._shadow_merge_decision`（纯决策缝合点：serial_shadow off→None 不记；on→`parse_merge_result` 降级三态；fail-safe 坏 payload→unknown 不当代 clean）+ else 分支接线（fail-open：classify 异常/超时→记 unknown 不阻断 baseline 开 PR；shadow 是可观测层非安全层，「main 不碰」由 dev-agent 契约+离线 drill 7.1b 保证）+ 记 `rec["shadow_merge_decision"]` + journal 事件 `shadow_merge_decision`（owner_repo+decision）作 canary parity 对照基线。**shadow parity 口径**：cutover.py 的 `run_shadow_parity_evidence` 是 durable-loop journal 域（≠ single-flight）；single-flight shadow 决策走独立 dispatch journal 事件（shadow 须产出可观测决策信号，否则 canary=开盲盒）。**ADR-0008 护栏#7 安全门落地**：`coordinator._FLAG_DEPENDENCIES` 加 `single_flight_auto_merge⇒single_flight_serial_shadow`（preflight 阻断「auto_merge on 但 serial_shadow off」禁用组合，防并发同仓 merge chaos；docstring gating 的运行时强制）。TDD 8 测（build_classify_cmd 2 + 真实 git classify-only 1[main 未碰+marker 无+worktree 还原] + preflight 安全门 reject/accept 2 + _shadow_merge_decision 门控/fail-safe 3）；quality 1444 passed（+8，xfail 仍 5）。
- [x] 7.1b **离线 merge drill**（canary 前必做）：fixture 真实 git tmp repo（注入故意红 commit），跑 merge→push→post-merge-test→revert 全链路，断言 main 回干净 + revert commit 干净 + marker 可 grep——补 shadow 测不到的核心链路 — **merge 阶段 ✓**（`test_dev_agent_merge.py` 3 测，subprocess 真实 git tmp repo，1331 passed）：CLEAN[--no-ff+ff-only push+marker footer 可 grep+双 parent 历史] / CONFLICT[main 未碰+rebase --abort 后工作树清干净] / UNKNOWN[fetch 失败 main 未碰]；**post-merge-test→revert 全链路 ✓（task 4.x apply 2026-07-30，+6 测）**：post-merge PASS[main 绿→merged]/FAIL[触发 revert]/UNKNOWN[无 test_cmd·checkout 失败→halt] + revert REVERTED[main 回绿+revert commit+双 parent]/CONFLICT[main 未碰]/UNKNOWN[merge_commit 缺→main 未碰]；9 测全链路（merge 3 + post-merge/revert 6，1372 passed）
- [~] 7.2 canary：cc-web-control 单项目开真实自动 merge，观察 N 次闭环（含一次故意红的 auto-revert + 一次熔断触发） — **执行流程就绪（RUNBOOK §8.3），待运维显式 go**：canary 把真实 commit 合进 cc-web-control `main`（outward、留真实历史、破坏性）——与可逆代码工作属不同类别，须运维显式授权 + 真实前置（PRD 候选/分支保护 ff-only/`PA_GITHUB_TOKEN` 写权限）就位后执行，不由本编码会话自动跑（守「outward/hard-to-reverse 须确认」+ `pa-test-no-dirty-data` 隔离 state/临时 log/unset PA_HEARTBEAT）。执行：双 flag on（`PA_SINGLE_FLIGHT_SERIAL_SHADOW=1 PA_SINGLE_FLIGHT_AUTO_MERGE=1`，preflight 要求 serial_shadow on）+ `run_daily.py --limit 1 --project cc-web-control`（隔离 state），观察 (a)正常闭环 merge+post-merge PASS / (b)故意红 auto-revert REVERTED main 回绿 / (c)同 PRD 再投 circuit breaker 熔断 / (d)无 halt/CRITICAL；merge_loop/main_status/alerts journal 事件闭合无 open intent + 目标仓 main 最终绿 = 通过判据。代码侧全绿（quality 1444 passed），canary 前置（shadow 7.1a + 离线 drill 7.1b）已就位。**canary 一键执行就绪（apply 2026-07-30，待运维显式 go）**：`scripts/canary_automerge.sh`（子命令 prep/branch-protect/run-a/inject-red/check-c/verify；outward 子命令 `--go` 门控，守 outward 须确认 + pa-test-no-dirty-data 隔离 state/临时 log/unset PA_HEARTBEAT）+ `scripts/canary-green.prd.md`（判据 a 绿 smoke 载体）+ `scripts/canary-red.prd.md`（判据 b 故意红载体，文档化双绿约束）+ RUNBOOK §8.3.1。**prep 无害核对全绿（rc=0）**：step1-2（shadow 12 测 + 离线 drill 12 测点）+ flag env + CLI 参数（`--project`/`--state-dir`/`--no-notify`/`--inject-prd` 均真实存在非漂移）+ cc-web-control profile/本地仓/remote + gh 认证（jyf2100）+ main 双检出 bug 已修（59e780a/0437031）。**分支保护实测推翻 direct push（2026-07-30 canary inject-red）**：cc-web-control main `required_pull_request_reviews`（0 reviews）+ `enforce_admins=true` → owner direct push main **被 GH006 拒**（"Changes must be made through a pull request"）；原「实测 direct push 可过」判断**错**——`7b35bb6` 是 PR #50 merge commit footer，**非** direct push 证据。→ pa direct push 路径整体被阻断（merge phase push main 被 GH006 拒 → `push_failed` → triage → auto-merge 不工作）。**修复（task §9 PR path 迁移）**：merge/revert 改 `gh pr merge --merge`（cc-web-control 实测可过：0 review 已满足 + `enforce_admins` 只拦直推不拦 PR 合并）。**判据 a 此前未真验证**：#50 是 baseline PR 合并（flag-off 路径），**非** pa direct push 闭环 → §8.1 判据 (a) 待 PR path 改完（task 9.1 ✓）后真跑（task 9.3，待运维 go）。**canary 仅剩判据 b（故意红 auto-revert）+ c（熔断）待真跑**——用户选定「先补齐前置、暂不真跑 outward」，脚本/PRD/RUNBOOK 就绪，待运维显式 go。**判据 b/c 在双绿约束下绕 dev loop**（dispatch 前置「dev+verify 双绿→才 merge」致 PRD 无法自然 post-merge 红）：inject-red 独立 worktree 注入红 merge commit（`--no-ff`+marker，复刻 dispatch 形态）→ post-merge-test FAIL → revert REVERTED；check-c 注入 cooldown 记录 → 再投 → 熔断门（完整 record→cooldown 闭环由 test_circuit_breaker 11 测覆盖）。**工作区**：cc-web-control 主工作区有 effort 重构（16 文件并行真实工作，勿清），canary 全程用 dispatch 独立 worktree 不受影响。
- [x] 7.3 回滚验证：`single_flight_auto_merge` 关 → 回到并发 + 开 PR 待 review 旧行为（已合 main 的 commit **不**自动撤回，靠 marker `git log --grep` 找出人工 revert） — **✓ apply 2026-07-30**：回滚是「flag off = baseline 不变」（design 决策#8）的结构性保证——整段 merge/revert/post-merge 闭环在 `dispatch_one` 的 `if _coord.flags.single_flight_auto_merge:` 门内（flag off 代码不跑），else 分支=baseline 兜底开 PR。**三不变式逐锁定**（与 auto_merge 接线本身的测试范式一致：dispatch_one 的 flag 门结构性 + dev-agent 契约测 + helper 测，非新造 dispatch_one 全桩测）：① flag off → baseline 并发投递+开 PR：`test_run_one_serial_shadow_off_no_slot_journal`（serial_shadow off→dispatch_one 照常调、无 slot journal）；② 已合 commit 不自动撤回：flag off 无 revert 代码路径跑（ADR-0008 护栏#5 结构性——无 `git revert` 在 baseline 分支），已合 commit 留 main；③ marker 可 grep 人工 revert：`test_merge_clean_no_ff_push_and_marker`（真实 git：merge commit footer `Pipeline-Merge: <prd_id>` 可 `git log --grep` 找出）。**shadow 决策也回滚**：7.1a `_shadow_merge_decision(False,...)→None`（`test_shadow_merge_decision_none_when_serial_shadow_off`）——serial_shadow off → 不记 shadow 决策、不跑 classify，纯 baseline。**运维流程落地 RUNBOOK §8.2**：关 flag（两级 rollback：auto_merge→开 PR，再 serial_shadow→并发投递）+ `git log --grep='Pipeline-Merge: '` 找已合 commit + `git revert -m 1` 人工撤 + halt/quarantine 仓须人工 triage 清 slot + CRITICAL alerts journal ack。

## 9. PR path 迁移（2026-07-30 canary GH006 修复）

> **背景**：§8.1 canary inject-red 暴露 ADR-0008 致命前提缺陷——pa merge/revert phase direct push main（`commit-tree`+`push <sha>:refs/heads/main`）在 cc-web-control（`required_pull_request_reviews` + `enforce_admins=true`）被 GH006 拒。用户选定方案 A「PR 完全替代 direct push」：删 direct push 代码，所有目标仓统一走 `gh pr create` + `gh pr merge --merge`，不新增 flag（`single_flight_auto_merge` 总开关不变）。

- [x] 9.1 `dev-agent.run_merge_phase` / `run_revert` direct push main → PR auto-merge — **✓ apply 2026-07-30**：新增 `_pr_automerge(head, main_ref, title, body)` helper（幂等 `gh pr list --head --state open --json number`→已有则 merge 已有 / 无→`gh pr create` 后 merge；`gh pr merge --merge` 双 parent；返回 `fetch+rev-parse origin/main` = **PR merge 后 main tip**，守 D12 ancestry 非 feature tip）。**merge**：CLEAN 后 `push --force-with-lease branch`（rebased 临时分支，D7 force 例外）+ `_pr_automerge(branch, ...)`（title 含 `Pipeline-Merge: <prd_id>`）。**revert**：detached HEAD 上 `git revert -m 1`→建 `pa-revert/<prd_id>` branch→`push --force-with-lease`+`_pr_automerge(...)`（title 含 `Pipeline-Revert`）→记 PR merge 后 main tip 为 revert_commit。**sha 陷阱锁定**：merge_commit/revert_commit 记 PR merge 后 main tip（非 feature/revert branch tip）。删除 direct push 代码（commit-tree+push main / push revert_sha:main 两处）。**TDD**：`test_dev_agent_merge.py` 加 fake gh（PATH 注入 + autouse fixture，subprocess 透明命中；fake gh 模拟 GitHub `--no-ff` PR merge：真实 git `commit-tree` 双 parent+`push main`），12 测全绿 + **mutation 验证**（移除 PR title marker→CLEAN 测 RED，证 marker 来自 PR title via fake gh pr merge，非 direct push 两行 `-m`）。merge_phase.py/run_daily.py dispatch **逻辑零改动**（JSON schema 不变：merge_commit/push_failed/revert_commit），仅 docstring 同步。quality merge 测 + ruff + compileall 全绿。
- [x] 9.2 规约同步 — **✓ apply 2026-07-30**：spec.md「Automated merge」requirement 重写（direct push → PR create+merge + marker in PR title + feature branch force-with-lease 例外 + main 禁 force + GH006 原因；scenario「Push fails」→「PR create/merge fails」）+ ADR-0008 决定段/护栏#2（PR auto-merge `--merge` 双 parent + direct push 弃用 + feature force 例外）/#8（PR-review 保护→走 PR 非 禁；必过 CI 才禁）+ design D7（PR auto-merge）+ Risk [main 分支保护]（canary 推翻 direct push + PR path 缓解）+ tasks 7.2 修正（删「实测 direct push 可过」错误）+ 本节 §9。fail-safe-dispatch/verified-dev-execution delta **无需改**（ancestry-based 措辞与 PR path 兼容）。
- [ ] 9.3 canary 判据 (a) 真跑（PR path 改完后） — 待运维显式 go（outward/破坏性，守须确认 + `pa-test-no-dirty-data` 隔离 state/临时 log）：cc-web-control 双 flag on + `run_daily.py --limit 1 --project cc-web-control`（隔离 state），观察 PR auto-merge 正常闭环（PR create+merge+post-merge PASS+main 绿），补全 §8.1 判据 (a) 此前未真验证项。代码侧全绿（task 9.1），canary 前置（shadow 7.1a + 离线 drill 7.1b）已就位。
- [x] 9.4 `canary_automerge.sh` PR path 适配 + 缺陷修复 — **✓ apply 2026-07-30**（脚本逻辑改，`--go` 真跑仍 outward 待运维 go）：(1) **prep** direct push 误判 → PR auto-merge 可行性检查（`gh api .../protection` 解析 `required_approving_review_count` + `required_status_checks`：0 review+无 check=可过 / 有 check=阻塞退 triage / 有 approving review=阻塞）(2) **inject-red** `push HEAD:main`（被 GH006 拒）→ `gh pr create --head redfeat + gh pr merge --merge`（复刻 dispatch PR path，双 parent+marker in PR title；MC=PR merge 后 main tip）(3) **branch-protect** docstring 标注 PR path 后**非必需**（gh pr merge 可过 PR-review 保护；enforce_admins 只拦直推）+ restore PUT body schema ≠ GET 返回（已知限制，留极端保险）(4) **check-c** `record_revert` 加 `stamp_fn=lambda: datetime.now().isoformat()`（原缺必传 kwarg → TypeError 崩）(5) **verify** 判据 b grep 加 dev-agent `--phase revert` JSON 信号（`"outcome": "reverted"`/`revert_commit`）——inject-red 不经 run_daily 无 `🔴`/`↩️` emoji emit，原 grep 必误报「未出现」。**trap `$WT` 评估非缺陷**（WT 在 trap 前 mktemp 已定义 + `|| true` 容错）。bash -n 语法 OK；quality.sh 全绿（1453 passed，canary bash 不在质量门范围）。
