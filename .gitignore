# === pa 流水线运行时态（本地状态/日志/profiles，绝不入仓）===
.project-auto/

# === 雷达历史语料（微信公众号抓取+解读，版权/隐私顾虑，不入仓）===
Knowledge/

# === Python / 测试 / lint 缓存噪音 ===
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# === 系统 / 编辑器 ===
.DS_Store
Thumbs.db
.vscode/
.idea/

# === Claude Code 运行态（session/history/todos 本地私有，防未来泄漏）===
# 保留共享配置：.claude/agents/ .claude/commands/ .claude/skills/ .claude/settings.json
.claude/projects/
.claude/todos/
.claude/history
.claude/shell-snapshots/
.claude/statsig/

# 例外：rocketmq-produce-consume skill 含本地 RocketMQ endpoints 与 node 运行态（node_modules），整体不入仓
.claude/skills/rocketmq-produce-consume/

# === 通用备份 / 临时 / 日志 ===
*.bak
*.tmp
*.log
