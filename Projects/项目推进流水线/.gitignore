# 运行时状态（每次跑流水线都会变化，不入库）
.project-auto/
.quality-evidence/
.runtime-evidence/

# Python 编译缓存
__pycache__/
*.pyc

# 备份文件
*.bak*

# drill 运行态产物（含本机绝对路径 → 跨机器不可复核；P1-2 改走 CI artifact store）
runtime-evidence-index.json
